{
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "eslint": "latest"
  }
}
